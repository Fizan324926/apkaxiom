[package]
name = "eocd-eval-rust"
description = "P1.9 §IV — Rust EOCD evaluator. Mirrors `Apkaxiom.Tv.EocdEval` Lean evaluator byte-for-byte; the validator diffs the two on the EOCD corpus."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
axiom-zip-ref = { path = "../../crates/axiom-zip-ref" }

[lints]
workspace = true

[[bin]]
name = "eocd-eval-rust"
path = "src/main.rs"
