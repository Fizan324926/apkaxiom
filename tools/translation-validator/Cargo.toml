[package]
name = "translation-validator"
description = "P1.2 translation-validator skeleton — runs Lean's `#eval` and the extracted Rust function on a fixed input set, asserts equality. Real semantic equivalence proofs land in P1.9."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true

[[bin]]
name = "translation-validator"
path = "src/main.rs"

[dependencies]
thiserror = { workspace = true }
axiom-extract-hello = { path = "../../crates/axiom-extract-hello" }

[lints]
workspace = true
