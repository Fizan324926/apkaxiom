# Copyright (c) APKAXIOM Authors. Apache-2.0 OR MIT.
#
# P1.9 translation-validator — runs the Lean LFH evaluator and the
# Rust LFH evaluator on the LFH corpus, asserts byte-identical
# JSON output line-for-line, fail-closed.

rust_binary(
    name = "translation-validator",
    srcs = glob(["src/**/*.rs"]),
    crate = "translation_validator",
    crate_root = "src/main.rs",
    edition = "2021",
    deps = [
        "//third-party/rust:thiserror",
    ],
    visibility = ["PUBLIC"],
)
