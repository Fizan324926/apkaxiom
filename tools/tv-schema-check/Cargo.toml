[package]
name = "tv-schema-check"
description = "P1.9 §V item 6 — validate evaluator JSON output against the canonical schema in docs/phase-1/P1.9/lfh-output-schema.json. Runs without pulling a third-party JSON-schema crate; the validator is hand-rolled to match the schema exactly."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[[bin]]
name = "tv-schema-check"
path = "src/main.rs"
