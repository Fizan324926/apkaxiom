[package]
name = "axiom-ir"
description = "AXIOM-IR v0.1 — frozen reference IR (core + manifest + resource dialects, canonical bytes, lowering, MLIR-style text)."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = { workspace = true }

[lints]
workspace = true
