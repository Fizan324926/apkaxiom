[package]
name = "axiom-zip-ref"
description = "P1.5 — Rust reference parser for ZIP local file headers + EOCD records. Mirrors theorems/Apkaxiom/Zip/{LocalHeader,Eocd}.lean byte-for-byte; the differential harness in tests/differential asserts equivalence on the 1500+ corpus inputs."
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
rust-version.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
thiserror = { workspace = true }

[lints]
workspace = true
